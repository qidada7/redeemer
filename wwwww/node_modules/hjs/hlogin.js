$(document).ready(function () {
	$("#miyao").hide();
	$("#ClickMe").hide();
	$(".ui button").hide();
});
$(function () {
	//用户登陆
	$("#tijiao").submit(function () {
		var uname = $("#username").val();
		var upaaword = $("#pswd").val();
		if (uname == "RE2020" && upaaword == "REDEEMER0" || uname == "RE150616" && upaaword == "REDEEMER6" || uname == "RE183929" && upaaword == "REDEEMER9" || uname == "RE204121" && upaaword == "REDEEMER1" && uname == "RE15457" || upaaword == "redeemer7" || uname == "RE199545" && upaaword == "XPHAHUE5" || uname == "RE88521" && upaaword == "XPHUYHH") {
			alert("登陆成功！各功能已被管理员开启，请放心使用！");
			$("#zhezhao").hide();
			$("#ClickMe").hide();
			$("#login").hide();
			$("#miyao").hide();
			$(".ui button").show();
		} else if (uname == "Re-admin" && upaaword == "redeemer3d") {
			alert("登陆成功！欢迎管理员:" + uname);
			alert( "请关闭遮罩后，点击多人图标分配账户并激活密钥！");
			$("#ClickMe").show();
			$("#miyao").show();
			} else {
				alert(" 账号或密码错误！")
			}
	})
})
//弹出隐藏层
function ShowDiv(show_div, bg_div) {
	document.getElementById(show_div).style.display = 'block';
	document.getElementById(bg_div).style.display = 'block';
	var bgdiv = document.getElementById(bg_div);
	bgdiv.style.width = document.body.scrollWidth;
	// bgdiv.style.height = $(document).height();
	$("#" + bg_div).height($(document).height());
};
//关闭弹出层
function CloseDiv(show_div, bg_div) {
	document.getElementById(show_div).style.display = 'none';
	document.getElementById(bg_div).style.display = 'none';
};